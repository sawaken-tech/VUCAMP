= render 'top/header'

.campIndex
  .campIndex__box
    - @camps.each do |camp| 
      .campIndex__box__url
        = link_to camp_path(camp) do
          .campIndex__box__name
            = camp.campName

.goNewPage
  = link_to new_camp_path do
    CAMP場登録へ

= render 'top/footer'